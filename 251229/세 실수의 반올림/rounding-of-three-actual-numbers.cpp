#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double a, b, c;
    cin >> a >> b >> c;

    cout << fixed << setprecision(3) << a << "\n";
    cout << fixed << setprecision(3) << b << "\n";
    cout << fixed << setprecision(3) << c << "\n";

    return 0;
}